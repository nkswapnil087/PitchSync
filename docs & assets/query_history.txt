PitchSync Oracle execution history
==================================

Reason:
Verify whether the local PITCHPDB connection supports operating-system authentication before V003 deployment.

Type:
SELECT / CONNECTION CHECK

Status:
FAILED

SQL:
SELECT SYS_CONTEXT('USERENV', 'SESSION_USER') || '|' ||
       SYS_CONTEXT('USERENV', 'CON_NAME')
FROM dual;

Oracle error:
ORA-12638: Credential retrieval failed.

Cause:
The PITCHSYNC_OWNER connection requires its database credential; operating-system credential retrieval was not available. No SQL reached the database and no object was changed.

Reason:
Perform the required read-only target and schema-state check before V003 deployment.

Type:
SELECT / CONNECTION CHECK

Status:
FAILED

SQL:
SELECT SYS_CONTEXT('USERENV', 'SESSION_USER') AS session_user,
       SYS_CONTEXT('USERENV', 'CON_NAME') AS container_name
FROM dual;

Oracle error:
ORA-12546: TNS: permission denied.

Cause:
The sandbox initially blocked the local listener connection. No SQL reached the database and no object was changed. The check was retried with approved local access.

Reason:
Retry the required read-only target and schema-state check with approved access to localhost:1522/PITCHPDB.

Type:
SELECT / CONNECTION CHECK

Status:
FAILED

SQL:
SELECT SYS_CONTEXT('USERENV', 'SESSION_USER') AS session_user,
       SYS_CONTEXT('USERENV', 'CON_NAME') AS container_name
FROM dual;

Oracle error:
ORA-01017: invalid username/password; logon denied.

Cause:
Oracle rejected the command-embedded CONNECT path before creating a session. The same credential later succeeded through the Oracle 19c password prompt, confirming that the password value was not the problem. No SQL reached the database and no object was changed; the password itself was not logged.

Reason:
Verify the corrected Oracle 19c password-prompt connection path and confirm the target before executing schema SQL.

Type:
SELECT / CONNECTION CHECK

Status:
SUCCESS

SQL:
SELECT SYS_CONTEXT('USERENV', 'SESSION_USER') AS session_user,
       SYS_CONTEXT('USERENV', 'CON_NAME') AS container_name
FROM dual;

Result:
SESSION_USER = PITCHSYNC_OWNER
CONTAINER_NAME = PITCHPDB

Cause / note:
The connection succeeded through the Oracle 19c SQL*Plus password prompt at localhost:1522/PITCHPDB. The password was read fresh from `.env.oracle-local`, sent only through standard input, and was not printed, placed on the command line, or persisted.

Reason:
Confirm that the authoritative target schema is clean before fresh V003 deployment.

Type:
SELECT / PREFLIGHT

Status:
SUCCESS

SQL:
SELECT 'TABLES' AS object_kind, COUNT(*) AS object_count FROM user_tables
UNION ALL
SELECT 'SEQUENCES', COUNT(*) FROM user_sequences
UNION ALL
SELECT 'TYPES', COUNT(*) FROM user_types;

Result:
TABLES = 0
SEQUENCES = 0
TYPES = 0

Reason:
Begin fresh V003 deployment after the authenticated target and empty schema were confirmed.

Type:
DDL / CREATE TYPE

Status:
FAILED

SQL:
CREATE TYPE address_type AS OBJECT (
    address_line      VARCHAR2(255),
    upazila_or_thana  VARCHAR2(100),
    district          VARCHAR2(100),
    division          VARCHAR2(100)
);

Oracle error:
ORA-01031: insufficient privileges.

Cause:
PITCHSYNC_OWNER does not have the CREATE TYPE system privilege required by V003. This is the first creation statement, so no V003 object was created and no later schema, index, trigger, or seed SQL was executed.

Reason:
Verify the failed CREATE TYPE left the schema unchanged and identify the account's effective system privileges.

Type:
SELECT / POST-FAILURE DIAGNOSTIC

Status:
SUCCESS

SQL:
SELECT 'TABLES' AS object_kind, COUNT(*) AS object_count FROM user_tables
UNION ALL
SELECT 'SEQUENCES', COUNT(*) FROM user_sequences
UNION ALL
SELECT 'TYPES', COUNT(*) FROM user_types;

SELECT privilege FROM user_sys_privs ORDER BY privilege;

Result:
TABLES = 0
SEQUENCES = 0
TYPES = 0
Granted privileges: CREATE PROCEDURE, CREATE SEQUENCE, CREATE SESSION, CREATE TABLE, CREATE TRIGGER, CREATE VIEW.
Missing privilege required by V003: CREATE TYPE.

Reason:
Resume deployment after CREATE TYPE was granted and reconfirm the safe clean-schema checkpoint.

Type:
SELECT / PRIVILEGE AND PREFLIGHT CHECK

Status:
EXECUTED SUCCESSFULLY

SQL:
SELECT privilege
FROM user_sys_privs
WHERE privilege = 'CREATE TYPE';

SELECT 'TABLES' AS object_kind, COUNT(*) AS object_count FROM user_tables
UNION ALL SELECT 'SEQUENCES', COUNT(*) FROM user_sequences
UNION ALL SELECT 'TYPES', COUNT(*) FROM user_types;

Result:
CREATE TYPE was present. TABLES = 0, SEQUENCES = 0, TYPES = 0. The authenticated target remained PITCHSYNC_OWNER in PITCHPDB on localhost:1522.

Reason:
Create the V003 core objects while safely skipping the destructive drop preamble on the confirmed clean schema.

Type:
DDL / V003 CORE CREATION

Status:
FAILED

SQL:
The unchanged creation section of database/migrations/V003_create_final_schema.sql, beginning with CREATE TYPE ADDRESS_TYPE. The DROP preamble was skipped.

SQL*Plus error:
SP2-0734 errors began when the multiline CREATE TABLE PERSON statement reached blank lines.

Cause:
SQL*Plus standard-input parsing had SQLBLANKLINES disabled. Both object types and five explicit sequences were created before the first table buffer was split; no table was created. This was an execution-client issue, not a V003 design or privilege error.

Reason:
Return the interrupted V003 attempt to the verified clean checkpoint before retrying.

Type:
DDL / TARGETED CLEANUP

Status:
EXECUTED SUCCESSFULLY

SQL:
DROP SEQUENCE seq_player_person;
DROP SEQUENCE seq_admin_person;
DROP SEQUENCE seq_match;
DROP SEQUENCE seq_complaint;
DROP SEQUENCE seq_case_record;
DROP TYPE education_type;
DROP TYPE address_type;

Result:
Only the five sequences and two types created by the interrupted attempt were removed. The follow-up catalog check returned TABLES = 0, SEQUENCES = 0, TYPES = 0.

Reason:
Deploy the authoritative V003 core from the restored clean checkpoint with correct multiline SQL*Plus parsing.

Type:
DDL / V003 CORE CREATION

Status:
EXECUTED SUCCESSFULLY

SQL:
SET SQLBLANKLINES ON;
The unchanged creation section of database/migrations/V003_create_final_schema.sql, beginning with CREATE TYPE ADDRESS_TYPE. The DROP preamble was skipped because the schema was clean.

Result:
2 object types, 5 explicit sequences, and 32 tables were created. SQL*Plus exit code 0.

Reason:
Install the audited V003 lookup/performance indexes and compatible validation triggers.

Type:
DDL / INDEXES AND TRIGGERS

Status:
EXECUTED SUCCESSFULLY

SQL:
@database/indexes/create_indexes.sql
@database/triggers/trg_person_dob_valid.sql
@database/triggers/trg_investigates_admin_role.sql

Result:
15 indexes and 2 triggers were created. Both triggers are enabled and all 15 explicit indexes are valid.

Reason:
Load and commit the audited V003 presentation dataset.

Type:
DML / SEED DATA

Status:
EXECUTED SUCCESSFULLY

SQL:
@database/seeds/v003_demo_data.sql
COMMIT;

Result:
226 inserts across 31 tables were committed. AUDIT_LOG intentionally remained empty for runtime activity.

Reason:
Run the first live structural and row-count verification after deployment.

Type:
SELECT / VERIFICATION

Status:
FAILED

SQL:
@database/tests/verify_v003_schema.sql

Oracle error:
ORA-00904: "TABLE_NAME": invalid identifier.

Cause:
The row-count UNION ALL report ordered directly by its projected alias. The verification-only query was wrapped in an outer SELECT so Oracle could resolve TABLE_NAME consistently. No database object or data was changed.

Reason:
Rerun the corrected structural verification and record the live deployment state.

Type:
SELECT / VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
@database/tests/verify_v003_schema.sql

Result:
32 tables; 32 primary keys; 41 foreign keys; 11 unique constraints; 138 enabled and validated Oracle check/NOT NULL constraints, including 6 named business checks; 226 total rows; 5 explicit sequences; 2 object types; 15 valid explicit indexes; 2 enabled triggers; 0 invalid objects.

Reason:
Execute all current V003 query files with repeatable seed bind values.

Type:
SELECT / QUERY SUITE

Status:
FAILED

SQL:
@database/tests/run_v003_queries.sql

SQL*Plus error:
SP2-0552: Bind variable "DATE_TO" not declared.

Cause:
This SQL*Plus 19c client does not support DATE in the VARIABLE command. The runner now declares the three date inputs as VARCHAR2(10), sets NLS_DATE_FORMAT to YYYY-MM-DD for its session, and the affected query casts date-range binds explicitly where arithmetic is required.

Reason:
Retry the complete V003 query catalogue after correcting the SQL*Plus runner.

Type:
SELECT / QUERY SUITE

Status:
FAILED

SQL:
@database/tests/run_v003_queries.sql

Oracle errors:
A05_system_record_counts.sql: ORA-00904: "METRIC": invalid identifier.
A07_audit_activity_report.sql: ORA-00932: inconsistent datatypes: expected TIMESTAMP got NUMBER.
A10_soft_deleted_record_summary.sql: ORA-00904: "ENTITY_NAME": invalid identifier.

Cause:
A05 and A10 ordered UNION ALL results by an alias Oracle did not expose at that level; both now use ORDER BY 1. A07 added one day to a SQL*Plus string bind, causing numeric coercion; its range binds are now cast to DATE. The schema and seed data were unchanged.

Reason:
Execute the fully corrected V003 query catalogue and confirm every current query runs against live seed data.

Type:
SELECT / QUERY SUITE

Status:
EXECUTED SUCCESSFULLY

SQL:
@database/tests/run_v003_queries.sql

Result row counts:
A01=6, A02=8, A03=3, A04=2, A05=7, A06=8, A07=0, A08=6, A09=6, A10=10.
P01=1, P02=1, P03=1, P04=2, P05=3, P06=2, P07=6, P08=2, P09=3.
T01=3, T02=4, T03=6, T04=4, T05=2, T06=2, T07=1, T08=0, T09=0.
Q01=5, Q02=4, Q03=2, Q04=2, Q05=2, Q06=1, Q07=2, Q08=2, Q09=2, Q10=1, Q11=6, Q12=1, Q13=1, Q14=1, Q15=1.

All 43 query files executed. Zero ORA-, SP2-, or PLS- errors were observed. Zero-row results are valid empty-state findings for the seeded filters, not execution failures.

Reason:
Capture the final live Oracle catalog and per-table row-count snapshot for deployment documentation.

Type:
SELECT / FINAL VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
Queries against USER_TABLES, USER_CONSTRAINTS, USER_SEQUENCES, USER_TYPES, USER_INDEXES, USER_TRIGGERS, USER_OBJECTS, and COUNT(*) for every V003 table.

Result:
SESSION_USER = PITCHSYNC_OWNER; CONTAINER_NAME = PITCHPDB; 32 tables; 226 committed rows; every PK/FK/unique/check constraint enabled and validated; 15 explicit indexes valid; 2 triggers enabled; 0 invalid objects.

Reason:
Activate the existing V003 local USER_ACCOUNT rows for database-backed application authentication.

Type:
UPDATE / LOCAL APPLICATION ACCOUNT SETUP

Status:
EXECUTED SUCCESSFULLY

Target:
PITCHSYNC_OWNER@localhost:1522/PITCHPDB

SQL:
UPDATE user_account
SET password_hash = :passwordHash,
    account_status = 'ACTIVE',
    is_deleted = 0
WHERE username = :username;

Result:
Eight existing V003 seed accounts were updated and committed as ACTIVE with bcrypt password hashes. No usernames, passwords, hashes, or Oracle credentials were added to this history.

Reason:
Verify database-backed login, selected-role enforcement, session cookies, server-side API authorization, player self-access, and sign-in timestamp persistence.

Type:
SELECT / UPDATE / AUTHENTICATION VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
The documented active-login account lookup and successful-login timestamp update in database/BACKEND_QUERY_GUIDE.md.

Result:
Correct account/role combinations authenticated; a mismatched selected role returned 401; an unauthenticated protected API returned 401; an unauthorized role and cross-player request returned 403; permitted resources returned 200. Successful login timestamps were committed for the tested accounts. No secrets were logged.

Reason:
Verify the backend player create, update, structured-address, collection, audit, authorization, validation, and soft-delete workflow.

Type:
INSERT / UPDATE / SELECT / TRANSACTION VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
The documented player write transaction statements in database/BACKEND_QUERY_GUIDE.md, executed through the authenticated Next.js APIs with bind variables.

Result:
A temporary verification player was created across PERSON and PLAYER, read through the detail API, updated with structured address and achievement values, read again, and soft-deleted. The create returned 201, update and delete returned 200, and the soft-deleted record subsequently returned 404. Invalid future-date input returned 400 before database execution, and a Team Performance Manager write attempt returned 403. The verification record remains only as soft-deleted history and the committed AUDIT_LOG rows record INSERT, UPDATE, and DELETE. No secrets were logged.

Reason:
Verify the role-aware dashboard reads against PITCHSYNC_OWNER on localhost:1522/PITCHPDB.

Type:
SELECT / AUTHENTICATED API VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
The dashboard queries documented in database/BACKEND_QUERY_GUIDE.md together with the previously documented registry/detail queries reused by GET /api/dashboard.

Result:
All six approved roles authenticated and received HTTP 200 from the dashboard API. Every response returned four live Oracle-backed metrics and correctly shaped role-specific tables; player results were restricted to the signed-session person ID. No secrets were logged.

Reason:
Final submission smoke verification of the authenticated player create, update, read-back, audit, and soft-delete transaction path.

Type:
INSERT / UPDATE / SOFT DELETE / SELECT / TRANSACTION VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
The documented bind-variable player write and audit statements in database/BACKEND_QUERY_GUIDE.md, executed through the authenticated Next.js APIs, followed by a direct read-only Oracle verification query.

Result:
Temporary player 100041 was created, read back with structured address and collection values, updated, and soft-deleted. Direct verification against PITCHSYNC_OWNER in PITCHPDB confirmed PERSON and PLAYER are deleted, all form-managed child rows are inactive, and exactly three PLAYER audit operations were recorded. The record remains only as soft-deleted audit history. No secrets were logged.

Reason:
Rename the eight local PitchSync application usernames while preserving every existing password hash.

Type:
UPDATE / INSERT AUDIT / AUTHENTICATION VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
UPDATE user_account SET username = :newUsername WHERE account_id = :accountId;

INSERT INTO audit_log (actor_person_id, entity_name, record_identifier, operation, old_values, new_values)
VALUES (200001, 'USER_ACCOUNT', :recordIdentifier, 'UPDATE', :oldValues, :newValues);

Result:
Renamed the active accounts to superadmin, boardadmin, performancemanager, matchofficial, integrity1, integrity2, player1, and player2 on PITCHSYNC_OWNER in PITCHPDB. All eight password hashes were verified unchanged, all eight new username/role combinations authenticated successfully with the existing password, and the old super.admin username returned 401. No secrets were logged.

Reason:
Update the eight local PitchSync application account password hashes to the user-requested per-account local credential pattern.

Type:
UPDATE / AUTHENTICATION VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
UPDATE user_account
SET password_hash = :passwordHash,
    account_status = 'ACTIVE',
    is_deleted = 0
WHERE username = :username;

Result:
Updated the password hashes for superadmin, boardadmin, performancemanager, matchofficial, integrity1, integrity2, player1, and player2 on PITCHSYNC_OWNER in PITCHPDB. All eight username/role combinations authenticated successfully through /api/auth/login, and a negative login using the superadmin username without the required local password prefix returned 401. Exact password values and hashes were not logged.

Reason:
Refresh the local Oracle 19c PITCHSYNC_OWNER schema after pulling the latest project database files from origin/main.

Type:
SELECT / DROP / CREATE / INSERT / MERGE / VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
Read-only preflight:
SELECT SYS_CONTEXT('USERENV','SESSION_USER') AS session_user,
       SYS_CONTEXT('USERENV','CON_NAME') AS container_name,
       SYS_CONTEXT('USERENV','SERVICE_NAME') AS service_name
FROM dual;

SELECT username, account_status, default_tablespace, temporary_tablespace
FROM dba_users
WHERE username = 'PITCHSYNC_OWNER';

SELECT object_type, COUNT(*) AS object_count
FROM dba_objects
WHERE owner = 'PITCHSYNC_OWNER'
GROUP BY object_type
ORDER BY object_type;

SELECT table_name
FROM dba_tables
WHERE owner = 'PITCHSYNC_OWNER'
ORDER BY table_name;

SELECT COUNT(*) checks for each existing PITCHSYNC_OWNER table.

Cleanup:
DROP TABLE PITCHSYNC_OWNER."<existing_table>" CASCADE CONSTRAINTS PURGE;
DROP SEQUENCE PITCHSYNC_OWNER."<existing_sequence>";
DROP VIEW/PROCEDURE/FUNCTION/PACKAGE/PACKAGE BODY/TRIGGER PITCHSYNC_OWNER."<existing_object>";
DROP TYPE PITCHSYNC_OWNER."<existing_type>" FORCE;

Deployment:
The unchanged creation section of database/migrations/V003_create_final_schema.sql, beginning with CREATE TYPE ADDRESS_TYPE. The DROP preamble was skipped because the schema had already been explicitly cleaned.

@database/indexes/create_indexes.sql
@database/triggers/trg_person_dob_valid.sql
@database/triggers/trg_investigates_admin_role.sql
@database/seeds/v003_demo_data.sql
@database/migrations/V004__integrity_officer_access.sql
@database/seeds/V004__seed_integrity_access.sql

Verification:
DBA metadata checks for table names, object counts, invalid objects, explicit indexes, triggers, row counts, and total row count. The current V003 query catalogue was executed with @database/tests/run_v003_queries.sql after setting CURRENT_SCHEMA to PITCHSYNC_OWNER.

Result:
Preflight confirmed Oracle Database 19c, PITCHPDB open read/write, and PITCHSYNC_OWNER present. The previous schema contained 43 tables and 203 rows: older PitchSync tables including INTEGRITY_CASE, missing V003 USER_ACCOUNT and CASE_RECORD, plus unrelated banking/sample tables including ACCOUNT, BRANCH, CUSTOMER, LOAN, and EMPLOYEE. The local owner was rebuilt to the pulled project version. Final state: 33 tables, 2 object types, 2 triggers, 15 explicit indexes, 0 invalid objects, 228 total rows, V003 USER_ACCOUNT present with 8 disabled seed accounts, V003 CASE_RECORD present, and V004 INTEGRITY_OFFICER_ACCESS present with 2 rows. All current V003 catalogue queries executed successfully. No credentials, password hashes, or private values were logged.

Reason:
Refresh the eight local PitchSync application account password hashes to the simplified per-account classroom credential pattern.

Type:
UPDATE / AUTHENTICATION VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
UPDATE user_account
SET password_hash = :passwordHash,
    account_status = 'ACTIVE',
    is_deleted = 0
WHERE username = :username;

Result:
Updated the password hashes for superadmin, boardadmin, performancemanager, matchofficial, integrity1, integrity2, player1, and player2 on PITCHSYNC_OWNER in PITCHPDB. All eight username combinations authenticated successfully through /api/auth/login after the refresh. Exact password values and hashes were not logged.

Reason:
Grant the minimum schema privileges required by database/project-update-2 and deploy its function, view, procedure, and PL/SQL demonstrations.

Type:
GRANT / CREATE OR REPLACE / SELECT / PL/SQL / VERIFICATION

Status:
EXECUTED SUCCESSFULLY

SQL:
Administrator preflight confirmed SYS in CDB$ROOT and PITCHPDB open READ WRITE.

ALTER SESSION SET CONTAINER=PITCHPDB;
GRANT CREATE PROCEDURE TO PITCHSYNC_OWNER;
GRANT CREATE VIEW TO PITCHSYNC_OWNER;

@database/project-update-2/99_run_all.sql

SELECT object_name, object_type, status
FROM user_objects
WHERE object_name IN (
    'PU2_FN_PLAYER_TOTAL_RUNS',
    'PU2_V_PLAYER_CAREER_OVERVIEW',
    'PU2_PR_PLAYER_SUMMARY'
)
ORDER BY object_type, object_name;

Result:
The grants were applied only in PITCHPDB. The project-update-2 master script completed as PITCHSYNC_OWNER. PU2_FN_PLAYER_TOTAL_RUNS, PU2_V_PLAYER_CAREER_OVERVIEW, and PU2_PR_PLAYER_SUMMARY were created and verified VALID. The cursor and exception-handling demonstration blocks also completed successfully. No credentials or password values were logged.
