export function PageHeader({ eyebrow, title, description, actions }: { eyebrow?: string; title: string; description: string; actions?: React.ReactNode }) {
  return <header className="flex flex-col items-start justify-between gap-5 sm:flex-row sm:gap-8"><div>{eyebrow ? <p className="mb-2 text-[11px] font-bold uppercase tracking-[0.12em] text-[var(--primary)]">{eyebrow}</p> : null}<h1 className="heading-font text-3xl font-semibold leading-none tracking-[-0.01em] lg:text-[34px]">{title}</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-[var(--text-muted)]">{description}</p></div>{actions ? <div className="w-full pt-1 sm:w-auto sm:shrink-0">{actions}</div> : null}</header>;
}
